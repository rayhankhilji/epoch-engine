{
  "name": "@epoch/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Epoch runtime — runs worlds, persists them, and serves them over HTTP and SSE.",
  "scripts": {
    "dev": "node --watch --no-warnings src/main.ts",
    "start": "node --no-warnings src/main.ts",
    "sim": "node --no-warnings src/cli.ts"
  },
  "dependencies": {
    "@epoch/core": "*",
    "@epoch/llm": "*",
    "@epoch/world": "*"
  },
  "license": "MIT"
}
